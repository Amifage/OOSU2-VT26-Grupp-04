<Application x:Class="Medlem_Presentationslager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Medlem_Presentationslager"
             StartupUri="Startsida.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="HuvudBakgrund" Color="#FFFBCBD3"/>
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource HuvudBakgrund}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <SolidColorBrush x:Key="TextFarg" Color="Black"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{StaticResource TextFarg}" />
        </Style>

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Background" Value="#F5F6FA" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
        </Style>

    </Application.Resources>
</Application>
